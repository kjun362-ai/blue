#include <stdio.h>

int main () {
    int total = 89;
    float average =95.2f;

    printf ("평균: %f\n", average);
    printf("총점: %d\n", total);
    printf("총점: %d, 평균: %f", total, average);

    return 0;
}